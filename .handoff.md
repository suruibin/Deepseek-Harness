# Deepseek-Harness 桌面壳定制 — 新会话接力说明

> 用途:新开会话时,让 Agent 先读本文件即可无缝接手,无需重读完整历史。
> 读取方式:新会话第一条消息写「先读 /projects/Deepseek-Harness/.handoff.md,然后继续」。

## 一、项目概况

- **仓库**:`/projects/Deepseek-Harness`(包名 `dsh-desktop-electron`)
- **本质**:DeepSeek Harness Web GUI 的 Electron 桌面壳 —— 透明毛玻璃窗口 + 注入式主题定制
- **架构**:Electron main(`src/main.ts`)→ `webContents.executeJavaScript` 注入多个 TS 模板字符串脚本(全部在 `src/glass.ts`)→ tsc 编译到 `lib/` → 加载 http://127.0.0.1:3080(dsh web)
- **宿主 GUI**:http://127.0.0.1:3080(本机 dsh web 实例,复用不重启)

## 二、当前工作区状态(git)

- **工作区干净**。近期成果已上库：
  - `b195236` 性能优化五项（六点十八/十九：终端 IPC 合并、transcript 惰性截断、修复 worker 化、cookie 清理并行、git-status TTL 缓存）
  - `02639e3` launcher 复用/探活修复（六点二十：marker 全量扫描、无 token ready URL 探活）
  - `6ada084` 壳适配 dsh 0.1.2-rc.1 token 栅栏（六点二十一：401 签名复用判定、resolveExternalLaunchToken journal 取 token、复用窗口 token 播种；含六点二十死循环结论修正）
- **铁律:未经用户明确说"提交/上库",禁止 git commit/push**
- **环境注意**:全局 dsh=0.1.1-rc.2;部署的 AppImage(21:04)=本工作区构建(含上述全部提交);**dsh-update.timer 已 disable --now(2026-09-06,当晚 rc.1 自动更新引发事故后)**,升级 dsh 需手动 `npm i -g @deepseek-ai/dsh@<ver>` 并评估 ready 行格式/认证模型兼容性

## 三、已完成功能清单(近期成果)

1. **透明毛玻璃窗口**(Linux 模拟玻璃):主界面/设置界面/输入框/侧边栏/弹出层五个滑块(主题设置→界面毛玻璃),localStorage 键 `dsh-desktop-glass-{main,settings,input,sidebar,popup}`;弹出层 5-100(默认 7),其余 0-100
2. **背景透明度滑块**(0-100%,`dsh-desktop-glass-alpha` 体系)
3. **主题设置页排序**:背景壁纸→背景透明度→界面毛玻璃→颜色切换时间→桌面功能→悬停效果(`reorderWallpaper`,order -2/-1/0,MutationObserver 保持)
4. **毛玻璃家族配方**:
   - 弹出层/下拉菜单:`rgba(39,46,62,α)` + `blur 40px` saturate(140%),跟随弹出层滑块(7% 侧边栏配方,无颗粒)
   - 面板内控件(导航选中/悬停、选择按钮 `_selector`、select/input):**只半透明不加 blur**(防嵌套 blur 卡顿)
   - 回到底部按钮:`Md3f7G_toBottom` 磨砂蓝灰 + `translateY(88px)` 下移
   - 插话 dock `_7yHdaG_dock`:780px 与输入卡同宽
   - 后台任务菜单:`QsffPG_menu` 不再被 header `overflow:hidden` 裁剪
   - composer 菜单/气泡/代码块毛玻璃化
5. **「标题颜色切换时间」已改名「颜色切换时间」**(在 `src/glass.ts` featureControlScript 的 `labels.cycle`,约 3068 行)—— 直接改源头,不要用 DOM 替换 hack(React 会恢复)
6. **光标特效 select**:`max-width:200px` + `margin-left:auto` 靠右;原生弹层暗色靠 main.ts 的 `nativeTheme.themeSource='dark'`
7. **打包**:`npm run dist` → `release/DeepSeek.AppImage`(124MB)

## 四、开发调试环境

```bash
# 1. 备份(每次改动后)
bash /projects/bin/backup "<改动说明>"

# 2. 编译
cd /projects/Deepseek-Harness && npm run build

# 3. 启动开发实例(带 CDP 9333)
# 先杀用户跑的 AppImage 实例(占单实例锁):
pgrep -af "[e]lectron/dist/electron" | awk '{print $1}' | xargs -r kill -9
# 再启动(注意 WAYLAND_DISPLAY 必须是 wayland-1,否则起不来):
WAYLAND_DISPLAY=wayland-1 XDG_RUNTIME_DIR=/run/user/1000 nohup ./node_modules/.bin/electron . --ozone-platform=wayland --remote-debugging-port=9333 > /tmp/dsh-dev.log 2>&1 &

# 4. CDP 调试(helpers 在 /tmp,系统重启会丢,需重建 /tmp/cdp-helpers.mjs)
#    getPage/connect/evaluate/screenshot;端口 9333
```

**验证要点**:
- 主题设置打开流程:侧边栏「设置」按钮(`VOzbGW_trigger`)→ 导航列表出现「主题设置」→ 点击进入
- **面板状态会漂移**(脚本点击可能落到别的导航),验证脚本要重复点「主题设置」直到 `VOzbGW_active` 是它
- 改注入代码后**必须重启应用**才生效(executeJavaScript 在启动时注入)
- 测原生 select 弹层颜色:CDP 点击 select → 截屏 → PIL 像素分析(SELECT 下方区域;暗色=rgb(64,54,71)+蓝高亮 rgb(65,118,230) 即成功)

## 五、已知坑(务必先看)

1. **注入脚本 IIFE 内任何 ReferenceError 会让后续代码全部失效**(observer 不创建 → 壁纸排序/滑块挂载全挂)。改代码后重启应用并打开主题设置验证滑块存在。
2. **面板内控件不要加 backdrop-filter**(嵌套 blur 卡顿,渲染进程 CPU 98% 的教训);弹出层类(`_list_`/`_menu`/portal)可以加。
3. **文本改名直接改注入脚本源头**,不要 DOM 替换+定时器(React 每 ~500ms 重渲染恢复,且 observer 里同步替换会死循环卡死)。
4. `/tmp` 会被清(cdp-helpers.mjs 重建即可);`/tmp/.mount_DeepSe*` 是 AppImage 挂载实例,占单实例锁。
5. 弹窗家族类名:`_3e4SsG_menu`(+号)、`_sideTop_`(Full access)、`_7KE1Ra_menu`(模型)、`JObwrW_panel`(上下文已用)、`_7yHdaG_dock/_panel`(插话)、`QsffPG_menu`(后台任务)、`Md3f7G_toBottom`(回到底部)、`_list_`(设置面板下拉 portal)、`VOzbGW_navCell`(设置导航项)。
6. localStorage 落盘技巧:SIGTERM 不触发 flush,用 CDP `Page.close` 优雅关页可 flush,重启后读新值。
7. **注入脚本是模板字符串,正则反斜杠会被模板字面量吃掉**:`ambientStyleScript` 等 `\`...\`` 里写
   `/^\d+px$/` 运行时变成 `/^d+px$/`、`\S`→`S`、`\s`→`s`,正则全错、静默返回 null。必须写
   **双反斜杠** `/^\\d+px$/`(模板求值后变单反斜杠)。tsc 源文本看着对、lib/glass.js 看着也对,
   但执行 `executeJavaScript` 前字符串已被模板求值过一次。改后要 `npm run build` + **重启**应用才生效。

## 六、待办/可能的方向

- 用户可能要求:继续微调滑块范围/样式、再次打包、或「上库」(届时才 commit+push)
- 已知观察:MEMORY.md 里记录过 dsh-glass-theme 插件 setWallpaper 不同步 DOM 的小缺陷(未修)
- 用户提到费用问题:长会话缓存读取量大(详见费用分析),新会话记得先读本文件+MEMORY 低成本起步
- **2026-08-22 资金清理**:用户要求清除所有会话。已备份全部会话到
  `/projects/Backup/dsh-sessions-20260822-111916/`(3.3G),随后清空 `~/.dsh/sessions/` 下所有会话
  并重置 `usage-stats-cache.json`,重启 dsh web + Electron 壳。费用大头=长会话缓存读取
  (老会话 5.65 天/2512 次调用/5.83 亿缓存 tokens),**务必及时开新会话**避免历史滚大。
- **2026-08-22 二次清理(执行于本会话)**:用户再次要求删除除当前会话外所有会话。核对 11:19 备份
  已覆盖全部 87 个待删会话(82 个 --projects-- + 5 个其他前缀,备份目录 `/projects/Backup/dsh-sessions-20260822-111916/sessions/`),
  直接删除,仅保留当前会话 `session-b88f4c23-c0d7-4921-9975-ea1ac4603f94`(3.3G→304K);
  重置 `usage-stats-cache.json`(旧值备份在备份目录下 `usage-stats-cache.json.bak`)。
  未重启 dsh web(重启会中断当前会话),GUI 刷新后历史列表即只剩当前会话。
- **2026-08-22 性能优化(执行于本会话,未 commit)**:src/glass.ts 9 处 MutationObserver 加 rAF 合并节流
  (alphaControl/attach/sessObs/sidebarRebuildObs/wallpaper ensure/wallpaperControl/featureControl/glassControl/heal,
  每处独立 schedule+requestAnimationFrame 包装,挂载类回调均有幂等守卫故安全);粒子上限 MAX_DROPS 160→80。
  顺带修复既有 bug:L1535 `toast('备份完成 ✓\n'...)` 在模板字符串里 \n 被解码为真实换行导致单引号字符串跨行
  → inputHistoryScript 注入即语法错误,输入历史+/backup 命令实际早已失效;改为 \\n 修复。
  验证:tsc 编译过 + 13 个注入脚本 new Function 语法全过 + CDP 实测(7 个注入标志全在、主题设置面板/滑块/壁纸/特性槽位
  挂载正常、200 次 DOM mutation 风暴无重复挂载、dev log 无注入错误)。改动前先备份。
- **2026-08-22 bug 修复:悬停效果开启后界面闪烁(执行于本会话,未 commit)**:根因=悬停下压的 transform 每帧变化 × 面板的
  backdrop-filter(侧边栏 ::before blur 24px / 头部 header blur 24px)= backdrop 每帧重新采样整块面板背后的内容 → 界面闪烁
  (与历史"嵌套 blur 卡顿 CPU 98%"同源)。修复(src/ambient.ts CSS 数组加 2 条规则):
  `html[data-dsh-hover-press] [data-dsh-aqua-spot][data-spot-on] { backdrop-filter:none !important; ... }` 及 ::before 同款,
  下压动画期间停用 blur,回弹结束后自动恢复(spot-on 移除即恢复)。CDP 实测:下压中 blur=none、结束后恢复 blur(24px)。
  注意:ambientDecor 在 ambientStyle 之后注入,同 !important 下新规则胜出。
  **v2 重做(用户反馈:停 blur 致界面颜色变化 + 仍闪)**:放弃停 blur 方案,改为:
  1) 辉光光斑背景渐变固定 50% 50%(glow 层放大 2*RADIUS 边距),位置用 transform:translate 每帧移动——合成器动画,零重绘,
     不触发 backdrop 重采样;2) 下压改停顿防抖:移动中只重置 80ms 定时器不更新 transform(移动期间保持上一角度),
     停顿 80ms 后才应用一次下压(单次 0.1s transition,平滑);3) blur 全程保留(颜色不变)。CDP 实测:blur 始终
     blur(24px)、glowBg 固定 50% 50% + transform 移动、下压移动中不更新、移出回弹正常。
  **v3 修复:辉光消失(用户反馈)**:根因=bg 用 dataset.glowCenter 一次性标记,首次 measure 设置 backgroundImage 后,
  一旦被 clearSpot/paint else 分支 removeProperty('background-image') 清掉,标记仍在 → 永不重设 → 光斑消失。
  修复:measure 里 bg 为空时重设(条件:dataset 未设 || bg 为空);clearSpot 不再清固定渐变(只清 transform);
  paint else 分支清 bg 时同时 delete dataset。CDP 实测:header/输入卡 on=true、opacity=1、bg 存在、transform 定位正常。
  注意:主题设置面板开着时(dialog 存在)sidebarCol 辉光被 tiltable 的 dialog 检查禁用,属设计行为。
  **v4 修复:悬停效果开启后界面闪(2026-08-22 下午,执行于本会话,未 commit)**:
  1) **TypeError 风暴(主因)**:onMove 在 session===null 但光标仍在 spot 内时把 null 传给 paint(),
     rAF 回调首行 s.spot 抛 `Cannot read properties of null (reading 'spot')`。触发场景=主题设置浮层打开
     (VOzbGW_mask/dialog 在 sidebarCol 内,onOver 对 sidebarCol 提前 return,session 永远 null)+
     鼠标在侧边栏移动 → ~100Hz 异常刷屏(CDP Runtime.exceptionThrown 实测),辉光更新全灭+渲染开销。
     修复:onMove 守卫改为 `spot===null || session===null || session.spot!==spot` 才 return(直接要求 session 非空)。
  2) **keeper 逐帧强制回流**:设置浮层打开期间每次 DOM 变化(拖滑块/流式输出)都执行
     transition:none + void offsetWidth 强制重排。修复:加守卫,仅 spot 确有残留 transform/transition 才清理。
  排查记录:第一轮捕获的"全屏 50% 变暗"其实是 VOzbGW_mask(rgba(0,0,0,0.5) 全屏遮罩,设置浮层自带)——
  用户当时开了设置浮层,非 bug;isolation:isolate 不会困住遮罩(sidebarCol DOM 靠后,画序本就在中心列之后,
  elementFromPoint A/B 实测悬停开/关遮罩都置顶)。修复后高频合成鼠标流(5ms 间隔)扫输入栏/侧边栏,
  辉光区亮度恒定、零异常。验证工具在 /tmp/hover-flicker-test*.mjs(重启会丢)。
  备份:.backup/20260822_hover-flicker/(沙箱拦 /projects/Backup,用的项目内目录)。

## 六点五、2026-08-22 优化 + 定制会话（执行于本会话，未 commit）

- **ponytail 清理**：main.ts 8 个 inject 包装函数 → 1 个 `inject(window, script)` helper + 调用表（注入顺序注释保留：themeSettings→alpha→wallpaper→feature→glass→ambientStyle→ambientDecor→inputHistory→whaleSpray；terminal/plugins 走原特殊路径）；删 `launchCwd()`、`glassCss`/`OPACITY_LEVELS`/`THEMES` 死导出、`electron-api.ts` 包装层（直连 `from 'electron'`）、空文件 `package.json.probe`。glassControlsScript 5 滑块 → SLIDERS 数组驱动。
- **新默认值（用户指定，固化在代码）**：背景透明度 0（`DEFAULT_ALPHA`=0）；界面毛玻璃 主界面 0 / 设置界面 5 / 输入框 30 / 侧边栏 5 / 弹出层 90（SLIDERS def）；悬停下压默认**关**（ambient.ts press fallback true→false）。glass-settings.json 已重置（alpha 0，壁纸保留）。localStorage 非壁纸键已清空（14 个键，壁纸 src/folder 保留）。
- **「弹出层模糊」滑块（新增）**：glassControlsScript SLIDERS 加 `{key:'popupblur', min:0, max:100, def:40, unit:'px'}` → `--dsh-glass-popup-blur`；滑块体系支持 unit 字段（% vs px）。
- **会话 hover 预览卡玻璃化**：`_card_1b2ny_13`（CSS modules）实心 rgb(44,44,46) 无 blur → 规则 `body > [class*="_card_"]`（body 直属通配，防 hash 变化失效），背景 `rgba(39,46,62,0.45)` + blur 跟随弹出层变量。扫描确认页面带背景 hover 浮层仅此一种。
- **backdrop-filter 失效根因与修复（重要）**：niri Wayland 无 Vulkan → Chromium 纯软件渲染静默丢弃 backdrop-filter（实测 blur 0 vs 100px 像素一致）。修复：main.ts 模块顶层 `app.commandLine.appendSwitch('enable-unsafe-swiftshader')` + `('use-angle','swiftshader')`（Linux only，GPU 进程创建前必须）。实测 blur 生效。dev/打包均覆盖。
- **验证注意**：CDP 打开主题设置面板需先点 `VOzbGW_trigger`（设置入口，element.click 有效）再点 `[data-dsh-theme-nav]`；「主题设置」文本节点有时不挂（healTick 依赖 body mutation）。菜单/composer 在滚动容器 `wSkVaW_scrollBody` 内，CDP 点击前需 scrollTop=scrollHeight。

## 六点六、2026-08-22 验证工具 + 性能修复（执行于本会话，未 commit）

- **渲染后端最终方案**：main.ts appendSwitch `use-angle=gl`（硬件 Mesa EGL，Linux only）。SwiftShader 实测 GPU 进程 300-660% CPU（卡），硬件 GL 仅 ~20% 且 blur 正常。无干预时纯软件渲染 backdrop-filter 静默失效。
- **性能优化**：大面积 blur 用新滑块「界面模糊」（SLIDERS `mainblur`，0-60px 默认 24，统一控制 main/settings/sidebar/input 的 blur 变量）。
- **嵌套 backdrop-filter 失效（重要坑）**：弹出层菜单（_sideTop_/_menu/_list_ 等）嵌套在输入卡片 uV2eYG_card 内，卡片自身有 backdrop-filter 时菜单 blur 静默失效（Chromium 内层采样祖先合成结果）。修复：**uV2eYG_card 规则移除 backdrop-filter**（只留半透明背景）。实测菜单 blur 差异 383→25506 px。以后新增浮层若"透明但不磨砂"，先查祖先链有没有 backdrop-filter。
- **弹出层默认值**：透明度 def 90→40（磨砂可见）。
- **CDP 验证工具（scripts/）**：
  - `scripts/cdp.mjs` — 参数化 CDP 客户端：`eval <js>` / `click <text>` / `hover x,y` / `scrollbottom` / `vars` / `shot <file> [x,y,w,h]` / `blur-test <a> <b>`（探针 blur 对比，一行出差异 px）
  - `scripts/dsh-run.sh` — GL + CDP 9222 一键启动（日志 /tmp/dsh-dev.log）
  - `scripts/dsh-kill.sh` — 干净杀进程（pgrep 排除自身，避免 pkill 自杀陷阱）
- **验证分级**（省 token）：滑块默认值/范围改动 → 只 typecheck + 注入语法检查；CSS 规则 → 编译 + cdp.mjs 验证；main.ts → dsh-kill + dsh-run 重启。
- **坑**：pkill -f "node_modules/electron" 会匹配自身 bash 命令行导致自杀 → 用 scripts/dsh-kill.sh。

## 六点七、2026-08-22 辉光跟随品牌色 + 半径调小（执行于本会话，未 commit）

- **辉光颜色跟随 DeepSeek 字标配色（新特性）**：glass.ts 新增 `publishBrandPalette()`——把当前字标渐变调色板 `gradColors` 发布为
  `--dsh-brand-color-1/2/3`（documentElement 内联），挂 3 个调用点：脚本初始化序列、`cycleBrandColors`（颜色切换时间轮换）、
  `forceBrandColorChange`（侧栏折叠强制换色）。ambient.ts 辉光渐变改为引用这三个变量（三段同心渐变 0%/35%/60%/transparent 72%）；
  var() 是活引用——字标配色每次轮换，已存在的辉光即刻换色，无需重设 backgroundImage。
- **透明度与颜色解耦**：原 `--dsh-aqua-spot-color`（写死蓝色 hsla216）废除 → `--dsh-aqua-glow-alpha`（暗色 0.17/亮色 0.16），
  颜色由品牌变量全强度提供、元素 opacity 统一压透明（on 态 opacity 从 1 改为 var(--dsh-aqua-glow-alpha, 0.17)）。
  兜底色 `GLOW_FALLBACK_COLOR='#5ad7ff'`（glass 未注入时）。dispose 同步改为清 alpha 变量。
- **辉光半径调小（用户要求"辉光小点"）**：GLOW_RADIUS 180→120→48（"再小60%"）。
- **辉光颜色模糊化（用户要求"颜色太清晰 模糊点"）**：glow 层加 `filter:blur(10px)`（合成器层一次渲染，transform 移动不重算，无性能损耗）+
  渐变 stop 拉长 35/60/72% → 45/75/100%——三色分段环带糊开、边缘缓慢消失。
- **验证**：CDP 注入新编译 ambientDecorScript（自带 dispose 替换旧实例）+ 手设品牌变量绿→粉（鼠标不动），
  悬停点 ±50px 均值色 delta R+24.5/G-15.3/B+11.3 = 绿→粉方向，活换色机制成立；radius=120 已入渐变串。
  **注意：运行中实例需重启应用才有 glass 侧自动轮换**（旧 glass 不发布变量，已手动同步当前字标配色应急）。
  验证工具 /tmp/glow-follow-test.mjs（重启会丢）。

## 六点八、2026-08-22 悬停效果整体移除（执行于本会话，未 commit）

- **用户决定：两个悬停效果（鼠标辉光 + 悬停下压）全部不要了**。整套移除：
  1. 删除 `src/ambient.ts` + `lib/ambient.js`（整个模块就是悬停效果引擎 + 设置开关）。
  2. main.ts：删 `import { ambientDecorScript }` 与注入表项（注入顺序注释同步简化；ambientStyleScript 保留——它是 glass.ts 的主题样式脚本，与悬停无关）。
  3. glass.ts themeSettingsScript：删 hoverSlot（`data-dsh-theme-hover-slot`）创建与挂载——主题面板不再有「悬停效果」分组。
  4. glass.ts：删 `publishBrandPalette()`（六点七为辉光加的品牌色变量发布，随辉光移除成死代码，3 个调用点+定义全删）。
  5. 运行实例 CDP 清理：dispose 后剥残留标记（data-dsh-aqua-spot/data-spot-on/data-dsh-inputbar/data-dsh-stats）、
     删 localStorage `dsh-desktop-hover-spotlight`/`dsh-desktop-hover-press`（实测 spots=0/glows=0/css=false/attr=false/keys=null）。
- **注意**：git 历史里 ambient.ts 仍可找回（含 v1-v4 全部闪烁修复记录，见上文第五/六点五节）；
  `.backup/20260822_hover-flicker/` 也有 ambient.ts 备份。若日后想恢复悬停效果，从这两处取。

## 六点九、2026-08-22 新会话界面错乱修复（执行于本会话，未 commit）

- **用户报告：新会话时界面是错的**。CDP 复现+定位出**两层根因**（都在 glass.ts keepComposerFloating）：
  1. **僵尸 seat（双输入框）**：钉底逻辑把 composer seat 挪出 scroll 到 centerCol 后，会话切换时 React 在原位置
     （scroll 内）**重建新 seat**，旧节点成孤儿（带钉底内联样式叠在页面底部）——实测 seatCount=2、textarea=2。
  2. **空会话布局错**：空会话 DSH 原生是输入框**垂直居中**的欢迎布局（实测 static、top≈577、h≈242）；钉底逻辑无差别
     生效 → 大片空白+输入框沉底，且 scroll 残留上一会话钉死的内联高度（1258px）。
- **修复（keepComposerFloating 重写）**：
  1. 僵尸清理：每帧先扫所有 `wSkVaW_composerSeat`——仅当 scroll 里存在原生 seat 且总数>1 时，删掉 scroll 外的孤儿
     （单节点在别处=正常钉底态，不能删）。
  2. 空会话检测：scroll 内除 seat 外无内容 → 解钉还原原生布局（清 scroll 的 flex/height/min-height 内联 +
     seat 挪回 scroll + 清 position/left/right/bottom/margin/z-index），都有"确有残留才清"守卫防逐帧强制回流。
  3. **坑：消息容器是 display:contents**（无盒高），`getBoundingClientRect().height` 恒 0——内容检测必须用
     "有高度**或有文本**"（`height>4 || textContent.trim()!==''`），纯高度检测会把消息会话误判成空。
- **验证（CDP 实测全循环）**：消息会话钉底（seat absolute 底部/scroll 1197px）→ 点新会话 → 原生居中（top 577/h 242/
  scroll 满高 1380/单 seat/零内联残留）→ 切回消息会话 → 重新钉底正常。三种状态 seatCount=1、textarea=1。
  验证工具 /tmp/reinject-glass.mjs（重注入 ambientStyleScript，重启会丢）。

## 六点十、2026-08-22 新会话+设置面板欢迎界面残留修复（执行于本会话，未 commit）

- **用户报告：新会话时点设置，新会话界面（欢迎布局）还一直在**。CDP 实测：设置 dialog（VOzbGW_panel 800×800，在
  sidebarCol 内）+ 全屏半透明遮罩（rgba(0,0,0,0.5)）打开时，中心列的欢迎 seat 仍显示（top 577 居中）——
  断开我们的 observer 复测行为一致，**属 DSH 原生行为**：半透明遮罩下透出居中输入框+设置面板叠着，视觉混乱。
- **修复（keepComposerFloating 空会话分支）**：dialog 存在（`[role=dialog]`）且空会话 → `seat.style.display='none'`
  隐藏欢迎界面；dialog 关闭 → 恢复 `''`。消息会话分支开头加还原守卫（`display==='none' → ''`），防设置打开期间
  切会话残留隐藏态。均带"确有变化才写"守卫。
- **验证（CDP 四路径全过）**：空+设置开→隐藏（h=0）✓；空+设置关→恢复居中（top 577/h 242）✓；
  设置开+切消息会话→display 还原+钉底（absolute bottom 1388）✓；关设置→消息会话钉底正常（scroll 1197px）✓。
- **坑**：设置面板关闭不是点 VOzbGW_trigger（那是开菜单），要点 `[class*=VOzbGW_close]`（aria-label=关闭）。

## 六点十一、2026-08-22 Session log 按钮点击无效修复（执行于本会话，未 commit）

- **用户报告：Session log 图标点击无效**。CDP 排查链：
  1. 按钮是 `nL4_yW_sessionLogButton`，`position:fixed; z-index:9999`，inline `left:152px; top:1336px`（DSH JS 设置）。
  2. 有我们的样式时按钮渲染在 (442,1350)——**落在输入区统计行下面**（seat sticky z-index:100 盖住，玻璃半透明所以图标
     透出来可见但点击落在统计行上）；原生样式下渲染在 (152,1336)（输入区左侧安全区，可点）。
  3. **根因**：glass.ts 的 header 规则直接给 `[class*="_centerCol"] header` 加了 `backdrop-filter`。backdrop-filter
     使元素成为 fixed 后代的**包含块**——按钮的定位基准从视口变成 header，整体右移 298px 沉到输入区底下。
     （另：backdrop-filter 同时创建堆叠上下文，把按钮的 z-index:9999 困在 header 内，斗不过 seat 的 z:100。）
- **修复**：header 玻璃改为 **::before 覆盖层模式**（centerCol/sidebarCol 同款）：header 本体只留
  `position:relative; background:transparent; border-radius; margin`，背景+blur 移到 `header::before`
  （absolute inset:0 / z-index:-1 / pointer-events:none / border-radius:inherit）。视觉完全一致，
  但 header 不再是包含块 → 按钮回到视口锚定 (152,1336) → 可点击。
- **验证**：elementFromPoint 命中按钮 SVG ✓；真实 CDP 输入事件点击 → 弹出「Session 导出已开始下载」对话框
  （按钮功能=导出会话 ZIP）+ 新增 13 个 DOM 元素 ✓；header::before computed backdrop-filter 生效（玻璃无回归）✓；
  tsc 零错误。
- **注意**：合成 click（.click()/dispatchEvent）不触发该按钮（React 处理器需真实事件序列），验证必须用
  Input.dispatchMouseEvent。工具 /tmp/test-sessionlog.mjs、/tmp/dom-diff*.mjs（重启丢）。
- **排查中的临时操作**：为做原生 A/B 曾整体移除 dsh-dt-style/dsh-glass-custom（用户看到效果消失），
  随 reinject-glass.mjs 重注入全部恢复，用户确认「正常了」「已经可以了」。

## 六点十二、2026-08-22 设置面板遮罩闪修复（执行于本会话，未 commit）

- **用户报告：还有概率闪（点设置/主题设置时 + 静止时偶发）**。排查证据链：
  1. DOM/布局层干净：layout-shift/longtask 探针 90s 静止期零事件，scroll 钉死高度零抖动。
  2. 截图轮询 300ms×90s：零闪帧。
  3. **帧级 screencast**（只在内容变化时推帧）抓到真凶：开关设置面板各有 **0.22/0.218 的全屏单帧跳变**（差异
     均匀分布全屏=全屏遮罩瞬时出现/消失，帧间无过渡）。根因：DSH 的 VOzbGW_mask 全屏遮罩 computed
     `transition: all` **无时长**（=0s 瞬时）；且关闭时 mask **DOM 直接移除**（非 display 切换，CSS 过渡无从谈起）。
- **修复（glass.ts ambientStyleScript）**：
  1. CSS：`@keyframes dshMaskIn/dshMaskOut` + `[class*=VOzbGW_mask] { animation: dshMaskIn 0.18s ease-out }`
     ——打开侧渐显。
  2. JS `maskFadeKeeper`（挂 obsTick 首位）：检测 mask 从有到无（observer 微任务→rAF 都在移除后首帧渲染前跑），
     同帧向 body 追加渐隐替身层（`data-dsh-mask-fade`，rgba(0,0,0,0.5) 全屏 + dshMaskOut 0.18s forwards，
     z-index 9990，animationend/600ms 双保险自删）——关闭侧视觉连续淡出。
- **验证（screencast 帧差 A/B）**：修复前 33 帧/8 个 0.2+ 跳变 → 修复后 **738 帧完整渐变序列/max 0.086/p99 0.053**，
  遮罩跳变消除。
- **遗留已闭环**：遮罩修复后用户确认「目前看没有出现闪的问题了」（含静止期偶发也未再现——全屏遮罩瞬时切换
  大概率就是静止期偶发闪的来源之一，因为 screencast 证明了静止期页面渲染层完全静止）。若后续复现再查
  GPU/合成器层（Electron GPU 参数/日志方向）。
- **环境坑**：①应用曾退出，`setsid nohup` 启动才能存活（普通 `&` 被终端会话清理连带杀掉）；②tsc watch 进程
  已死，改完必须手动 `npx tsc`（--noEmit 不输出 lib）；③/tmp 诊断脚本会被清理，本节工具：flicker-monitor.mjs
  （layout-shift/longtask/scrollH 探针）、screencast-settings.mjs（帧级 A/B）、reinject-glass.mjs（重注入）。

## 六点十三、2026-08-22 侧边栏偏蓝修复（执行于本会话，未 commit）

- **用户报告**：设置→主题设置→背景透明度 100% 时，侧边栏比右侧输出界面偏淡蓝，想要两边同色。
- **两层根因（CDP 逐层实证）**：
  1. **透明态**（alpha 0）：侧边栏玻璃滑块被设为 30%（`--dsh-glass-sidebar-bg=rgba(15,17,23,0.30)`）而主界面 5%，
     透明窗口下 30% 深蓝黑把桌面压成淡蓝（实测 sidebar (16,16,25) vs center (20,20,20)）。→ 用户把「界面毛玻璃→侧边栏」
     滑块调到 5%（与主界面一致，零代码），透明态立即同色。
  2. **不透明态残留偏蓝**（alpha 1、双滑块 0% 后仍偏 +3 blueShift）：根因=侧边栏 `::before` 的
     `backdrop-filter: blur(...) saturate(140%)`——饱和滤镜把深蓝黑底 rgb(15,17,23) 的蓝通道拉高到 (14,17,25)，
     而中心列同款规则实测未生效（(15,17,23)），故侧边栏恒定比中心蓝一点。
- **修复（src/glass.ts ambientStyleScript 侧边栏 ::before 规则，约 L745）**：去掉 `saturate(140%)`，只留 blur。
  隔离实验证明：去掉后 sidebar (15,17,23) = center (15,17,23)，delta 0.0。
- **验证**：npm run build（tsc 零错误）→ 重启 dev 实例 → CDP alpha=1 实测 sidebar::before=`blur(20px)`、
  侧边栏/中心中位 (15,17,23)/(15,17,23) 差值 0.0 PASS；用户确认「现在一样了」。
- **备份**：.backup/20260822_sidebar-blue/（glass.ts + 验证截图）。
- **环境**：CDP dev 实例仍跑在 9222（dsh-run.sh，用户真实 profile，alpha=1 已持久化，侧边栏滑块 5% 已写入 localStorage）。

## 六点十一、2026-08-29 省 token 经验（长会话烧 token 教训 + 新工具）

**背景**：turn 12（改弹窗/圆角/磨砂）一轮烧了 **2364 万输入侧 token**。根源不是模型输出，而是：

- **140 个工具步骤 × 每步重发递增的全历史**：每步 cacheReadTokens 从 9.6 万单调涨到 22.9 万，累加 = 2360 万（占 99.99%）。
- **最大浪费 = 反复猜 CSS 类名**（59 步）：拿 grep 换着正则试 hashed 类名（Find dock-related CSS tokens 连续 3 次、TodoPanel CSS 连试 6 步）。根因=当时没有一次抓全类名的工具。
- 次要浪费 = **electron 反复崩溃重启**（29 步）：GPU 进程崩了 pkill + 重启 + 等待 × 多次，每次都要重跑 build。

**新增工具（scripts/cdp.mjs 加 `classes` 命令）**——以后查界面类名永远一行搞定，别再 grep 猜：

```bash
# 抓页面所有 CSS 类名：<类名> <元素数> <宽度样本>，按出现次数排序
node scripts/cdp.mjs classes            # 全部
node scripts/cdp.mjs classes ztWv       # 只含子串的（精准定位某个组件）
node scripts/cdp.mjs classes _block     # 例：markdown 代码块类
```

**省 token 铁律（UI 调试类任务）**：
1. **找类名用 `classes` 命令，绝不靠 grep 猜**。一次拿到全部类名 + 计数 + 宽度，直接定位目标。
2. **调试循环拆到 subagent**：`改文件 → build → 重启 → CDP 实测 → 再改` 这类反复迭代交给独立 subagent 跑，它历史短，不把主会话几百万 token 带进每次工具调用。（本次 main 会话 140 步全部 run_code 亲为是失误。）
3. **复用已启动的 electron**：脚本里用 `scripts/dsh-kill.sh` + `dsh-run.sh` 管理，别手动 pkill 自伤；GPU 崩了先 `scripts/dsh-kill.sh` 清干净再 `dsh-run.sh`。
4. **预设「PTC 省Token」= code-save**（工具结果 >4096 字符修剪、60% 触发压缩）本身已在省，但挡不住"步骤数×递增历史"；最有效是**步骤降下来**，而不是靠修剪。

**各轮实测（本会话）**：turn11=299 万 / turn12=2364 万 / turn13=303 万；cacheRead 占比 98%~100%。

## 六点十二、2026-08-30 已归档面板横排分组 + 浏览器自动打开修复（执行于本会话，未 commit）

- **已归档面板改造（session-manage-client.ts，按用户最终确认）**：横向 chip 导航，**回收站也是 chip**：
  `全部（N） 工作区1（N） ... 回收站（N）`，点选过滤；条目在**固定高度**面板（卡片 height:560px +
  padding+border 外廓 ~602px，body flex:1 overflow-y:auto）内**直接竖直滚动**浏览全部条目（用户明确
  不要「第 1/3 页」分页、不要翻页滑块）。CDP 实测：chips=全部31/Code1/projects30/回收站15；点
  projects → 取消归档按钮 30 个 + chip 高亮；点 回收站 → 恢复按钮 15 个；body 523px 高、scrollH 872
  竖直滚动正常；hasPager=false。
- **问题3 生效确认**：workspace.json 原始 archivedSessionIds=38，回收站清单 15 条中 7 条与归档重叠
  （宿主内存残留），面板显示 31=38-7 —— archivedList 按回收站清单过滤，已删除只出现在回收站。
- **问题2 方案（用户选择）**：取消归档/恢复 不强推自动重启，改为面板右上角**手动「重启 dsh」按钮**
  （main.ts `restartWebServer` + `dsh:web-restart` IPC + preload `session.restartWeb`，原地按 /proc
  复制原 argv/cwd 重启并重载窗口）。toast 文案改为「需点重启 dsh 后更新」。
- **浏览器自动打开修复（用户反馈「启动还打开网页版 deepseek」）**：根因=`dsh web` 默认**自动打开默认浏览器**
  （实测 stdout 打印 `opening the default browser; pass --no-open to disable`）。修复=launcher.ts `WEB_ARGS`
  追加 `--no-open`。重启后日志确认 `dsh web --host 127.0.0.1 --port 0 --no-open` 且不再打印浏览器打开提示。
- **流式闪烁修复生效确认（用户反馈回答中悬停侧栏右侧闪）**：`streamingGuardScript`（glass.ts）在注入表内；
  实测：正文节点变更 → html[data-dsh-streaming]=1，静默 1s 后自动清空；dsh-dt-style 含
  `html[data-dsh-streaming] body > [class*="_card_"] { backdrop-filter:none !important }` —— 流式中悬停预览卡
  停止 blur，消除连续重绘引发的闪烁。
- 验证：npm run build（tsc 零错误）+ npm test（69 通过）；CDP 9333 实例已按
  `electron . --ozone-platform=wayland --remote-debugging-port=9333` 启动（/tmp/dsh-dev.log）。

## 七、用户偏好速查

- **始终中文回答**(代码/标识符除外)
- 弹出层:不要能看穿的透明、不要重颗粒,参考侧边栏 7% 淡罩
- 滑块统一 0-100%(弹出层 5-100)
- 主题设置排序:背景壁纸→背景透明度→界面毛玻璃→颜色切换时间→桌面功能（悬停效果已整体移除,2026-08-22）
- 备份每次改动;未经指示不 commit/push

## 六点十三、2026-09-05 模型选择弹出层 provider 标题行毛玻璃化 + 选项文字矩形底修复（执行于本会话，未 commit）

- **问题1（用户：provider 标题行背景是实体的 我需要毛玻璃效果）**：模型选择弹出层（composer `uV2eYG_row` 内 `_7KE1Ra_trigger` → `_7KE1Ra_menu` → 「模型」cell → 列表）中 DeepSeek / Ollama (Local) / 智谱 GLM (Zhipu AI) / FreeToken (本地 Qwen3.6) 四个分组标题行 `_7KE1Ra_groupTitle` 被 DSH 用实心 rgb(39,46,62) 绘制。前次会话曾把它们并入弹出层家族 var（0.38 叠加菜单 0.38，视觉仍为深色横带）；本会话改为 **`background: transparent !important`**——标题行位于已 blur 的菜单容器内部，自身 backdrop-filter 因 Chromium 嵌套丢弃不会渲染，文字直接坐在菜单毛玻璃上才是真正的毛玻璃。
- **问题2（用户：Qwen3.6 35B A3B (FreeToken) 背景有矩形 要去掉）**：家族选择器 `[class*="_7KE1Ra_option"]` 子串误命中选项内部标签 span `_7KE1Ra_optionCopy`（border-radius 0），给它叠了同色 0.07 方角底，形成每个选项文字后的可见矩形。修复=**`_7KE1Ra_optionCopy { background: transparent !important }`**，只保留外层 10px 圆角 chip。
- 改动文件：src/glass.ts（ambientStyleScript 注入表 +758~775 附近，仅增两条 transparent 规则并把 `_7KE1Ra_groupTitle` 移出家族规则）。
- 备份：.backup/20260905_model-grouptitle-glass/（glass.ts.pre-fix = 会话开始态含前次未提交的部分修复；glass.ts.post-fix = 本会话最终态）。
- 验证：npm run build / tsc --noEmit 零错误；npm test 69/69 通过；CDP 实测 4 个 groupTitle 与 7 个 optionCopy 计算背景均 rgba(0,0,0,0)；600 次合成鼠标流（~6.1ms/次）扫过弹层后静置双帧 diff=0px，弹层完好 → PASS。
- 注意：类名 `_7KE1Ra_*` 为 web 端 CSS-modules hash，web 升级重 hash 后选择器失效需重新抓取（cdp.mjs classes 可查）。

## 六点十四、2026-09-05 追加（同日第二次反馈：还是有矩形）

- 用户反馈选项行仍有矩形。实测排查（弹层内逐元素 computed style 扫描）：仅剩 `_7KE1Ra_option` chip 本身（239x38、10px 圆角、0.07 tint 叠菜单 0.07 ≈ 0.135 可见条块），悬停态无额外绘色。
- 最终方案：`_7KE1Ra_option` 也 **transparent**（列表完全扁平，模型名直接坐在菜单毛玻璃上，选中态靠 `_7KE1Ra_check` ✓ 图标），`_7KE1Ra_option` 从弹出层家族规则移除；悬停反馈单独保留 `[class*="_7KE1Ra_option"]:hover { background-color: var(--dsh-glass-popup-bg) !important }`。`_7KE1Ra_optionCopy` 保持 transparent（防悬停态出现方角块）。
- 验证：tsc 零错误；69/69 通过；弹层内除 `_7KE1Ra_menu` 自身外无任何绘色元素；600 次 ~6ms 鼠标流后 idle diff=0px PASS；hover 实测 hover=true bg=rgba(39,46,62,0.07)。
- 备注：dev 实例（9222）验证期间被外部关闭过两次（用户在操作窗口）；重开后复测通过。

## 六点十五、2026-09-05 实心 rgb(39,46,62) 清零：全部并入弹出层滑块家族（未 commit）

- 用户要求：项目中仍在用实心 rgb(39,46,62) 的表面全部改毛玻璃，统一由 设置→主题设置→界面毛玻璃 的弹出层滑块驱动。
- 排查（全 src 扫描）剩余实心共 6 处：SURFACE_DARK 五个别名(--dsw-alias-bg-layer-3 插件/配置卡片与输入、--dsw-alias-bg-module-platform 外观选中立方与徽章、--dsw-specific-menu 下拉菜单、--dsw-alias-button-floating-hover 悬浮按钮悬停、--dsw-alias-button-ghost-active-fill 消息气泡/状态徽章) + misc-scripts.ts 颜色切换分钟输入框内联样式。
- 改法：五个别名值改为 var(--dsh-glass-popup-bg, rgba(39,46,62,0.07))（DSH 消费处自动半透明、跟随弹出层滑块 5..100）；misc-scripts 输入框背景同改。其余 0.35/0.45/0.62 等固定 alpha 玻璃面（tooltip/卡片/悬浮卡等）按此前用户确认的可读性 alpha 保持不变。
- 验证：tsc 零错误、69/69 通过；CDP 实测 body 五个别名均为 var 引用，全页扫描无任何可见元素计算背景为实心 rgb(39,46,62)；模型列表弹层内仅菜单容器 rgba(39,46,62,0.07) 绘色；600 次 ~6ms 鼠标流 idle diff=0 PASS。

## 六点十六、2026-09-05 消息操作行矩形带修复：p-xYUq_action 子串误命中 _actions 容器（未 commit）

- 用户反馈：统计行(9月2日 23:32 · 用时 13秒 · 首 token 1.8秒 · 38 tok/s)与对话时间(02:03)背景有矩形，要全透明。
- 根因：弹出层家族规则 `[class*="p-xYUq_action"]` 子串误命中整行容器 `p-xYUq_actions`（含会话/消息操作行与时间行），容器被加上 backdrop-filter blur+saturate——背景虽透明，但整条 24px 高的行把壁纸磨砂成可见矩形带（elementsFromPoint 全链 bg 透明，bf=blur(20px) saturate(1.4) 定位）。
- 修复：动作规则改 `[class*="p-xYUq_action"]:not([class*="p-xYUq_actions"])`（_8_XoUG_action 同理），容器单独规则强制 `background:transparent + backdrop-filter:none`；"..." 操作按钮玻璃保留。
- 教训沉淀：attribute-contains 选择器对单复数同源类名（action/actions）必须 :not 排除容器。
- 验证：tsc 零错误、69/69、容器 bg=transparent bf=none、统计行/02:03 截图无矩形带、4 个操作按钮玻璃仍在、600 次 ~6ms 鼠标流 idle diff=0 PASS。
- 环境注意：验证期间 dsh web 孤儿进程占过 9222 端口致新实例 CDP 起不来，dsh-kill.sh + kill 孤儿后恢复；pkill -f "electron" 会自杀匹配中的 bash，勿在复合命令里直接 pkill。

## 六点十七、2026-09-06 认证 cookie 膨胀修复：随机端口启动 431 空白窗口（未 commit）

- **问题（用户反馈：外壳空白窗口打不开，但浏览器能打开同一 dsh）**：dsh 认证 cookie 名含 authority 哈希（`dsh-auth-<hash>`），而 cookie 不区分端口 —— 每次随机端口（`--port 0`）启动都在 `127.0.0.1` 这个 cookie jar 里沉积一个永不复用的旧 cookie，多次启动后 Cookie 请求头膨胀超过服务器请求头大小限制 → **431 → 空白窗口**。浏览器 cookie jar 独立，故同一服务器浏览器能打开。
- **修复**（`src/main.ts`，+23 行）：新增 `clearStaleAuthCookies()` —— 遍历 `session.defaultSession.cookies.get({})`，删除全部 `dsh-auth-*`；在 `boot()` 最开头调用。认证 cookie 由本次 URL 里的 launch token 立即重新种上，无副作用。
- **验证**（本会话补完，改动作于 Claude 会话 9f947e30，当时因工具超时未跑完验证）：`npm run build` 0 错误；`npm test` **72/72 通过**；保留膨胀 jar（53KB）直接启动，启动后 cookie jar 收敛为仅 1 条本次新种 cookie（`dsh-auth-HDAjrvNC...` 对应当前端口），窗口正常渲染（用户确认「看着可以了」）。
- 备份：`/projects/Backup/Deepseek-Harness/20260906/18_20_39/`（src/main.ts）。
- 备注：dev 实例验证后退出（exit 0 无错误日志，用户在操作窗口，疑似托盘手动退出）；`--remote-debugging-port` 需手动追加（run-electron.mjs 不带 CDP 参数）。

## 六点十八、2026-09-06 性能优化三项：终端 IPC 合并 + transcript 惰性截断 + 会话修复 worker 化（未 commit）

- **优化1 终端输出 IPC 合并**（`src/desktop-ipc.ts`）：node-pty 每 chunk（几十字节）逐条 `webContents.send` 改为按 tab 缓冲合并整包推送——16ms 定时器兜底（回显延迟上界）、满 64KiB 立即冲刷；`dsh:term-close`/`dsh:term-exit` 前先 flush 保序。实测 3.9MB 输出仅 59 条 IPC 消息（原先每秒数百条）。
- **优化2 transcript 惰性截断**（`src/pty-registry.ts`）：超 1MiB 后每 chunk `slice(-1MiB)` 全量拷贝（O(n²)，10MB 输出 ≈ 2.6GB 拷贝）改为超 2×limit 才批量裁回 1MiB（每 limit 字节才付一次拷贝，均摊 O(1)；内存上界 2×limit，回放长度 ∈ [limit, 2×limit)）。cap 测试同步改为 2× 契约。
- **优化4 会话修复 worker 化**（`src/session-repair.ts` + 新增 `src/session-repair-worker.ts` + `src/main.ts`）：zstd 解压/压缩与 JSONL 解析是同步 CPU 密集操作，主进程直跑会阻塞事件循环（修复期间所有 IPC/窗口交互停摆）；新增 `repairSessionLogsAsync()` 用 worker_threads 执行，worker 文件路径按编译产物解析（`lib/session-repair-worker.js`，vitest 下不触发）。main.ts 弃用 setImmediate+同步版。
- 验证：tsc 零错误；72/72 通过；worker 独立冒烟（`node -e` 起 lib 产物）回传报告正常；CDP 实测真实链路——echo 回显 3 条合并消息、`seq 1 500000` 3.9MB 仅 59 条消息且内容完整到 prompt、两轮大输出 transcript 长度 1791664/1240136 均在 [1MiB,2MiB) 且尾部 `300000\r`（PTY ONLCR 把 \n 翻成 \r\n，断言 needle 要带 \r）。
- 备份：`/projects/Backup/Deepseek-Harness/20260906/19_12_00/`（6 文件）。
- 备注：性能清单其余项（git-status 缓存、品牌色 tick、缩略图 worker 化等）见会话记录，未实施。

## 六点十九、2026-09-06 性能优化 5+6：cookie 清理并行化 + git-status TTL 缓存（未 commit）

- **优化5 cookie 清理不阻塞启动**（`src/main.ts`）：`clearStaleAuthCookies()` 从 boot 开头同步 await 改为与 server 探测/启动并发（`const staleCookiesCleared = ...` 不 await），仅在两条 createWindow 路径（复用实例 / 全新启动就绪后）前 `await` 兜底；内部逐 cookie 串行 remove 改 `Promise.all` 并行。顺带修复原代码 `cookie.path` 缺省时模板串塞 "undefined" 进 URL 的潜在小 bug（`path ?? '/'`）。
- **优化6 git-status TTL 缓存**（`src/git-status.ts` + `src/desktop-ipc.ts`）：新增 `gitStatusCached(cwd, options?)` —— cwd 为键、默认 1.5s TTL、并发 in-flight 去重、非 repo 结果同样缓存（repo 外浏览也不再 spawn）、上限 16 条最旧淘汰；TTL/时钟/存储均可注入（测试隔离）。`dsh:git-status` IPC 改走缓存版。文件面板每次导航/刷新重复查同一 workspace cwd 的完整 git 管线（4 进程 2 串行轮，~12ms）在 TTL 内收敛为 0 次spawn。**更正六点十八前的口头清单：git-status 本就是 2 轮（gate + 3 并行），非 4 串行；合并 rev-parse 因空仓/分离 HEAD 边界复杂未做，缓存已覆盖主要收益。**
- 验证：tsc 零错误；76/76 通过（新增 4 个缓存测试：TTL 命中/过期、并发去重、per-cwd 键、非 repo 缓存）；CDP 实测真实应用——`dshDesktop.git.status()` 首次 12.2ms → TTL 内 0.1ms → 过期后 12.4ms，branch=main 内容正确；启动后 cookie jar 仅 1 条本次新种 cookie（注：启动后 ~30s 内 Chromium 可能未把 cookie 落盘 SQLite，读盘 0 属正常延迟）。
- 备份：`/projects/Backup/Deepseek-Harness/20260906/19_24_26/`（4 文件）。
- 剩余未实施：品牌色每秒 tick（glass.ts:1277）、缩略图解码 worker 化、`[class*=]` 选择器收敛、就绪探测 250ms 轮询、窗口就绪前先建 loading。

## 六点二十、2026-09-06 dsh 无法对话事故定位与修复（未 commit）

- **根因链**：10:16 `dsh-update.timer` 自动更新全局 dsh 到 0.1.2-rc.1 → 19:26 排查时 pkill 误杀用户多天未重启的 3080 服务（内存中旧版代码正常）→ 晚间双实例冷启动 rc.1：shell 自起实例陷入 preset/YAML 解析死循环（CDP CPU profile 定位：Inbox/get/entries + js-yaml composeNode，100% CPU 卡死事件循环）→ shell 无法对话；网页版因服务重启 token 失效。
- **rc.1 附加坑**：部署 asar 为陈旧构建缺 `--no-open`（dsh 自开浏览器疑似抢占首访 → shell 探活 401）；`__DSH_BOOT__` marker 在 HTML ~14KB 处，壳的复用探测只扫前 4KB → `detectExistingServer` 从未生效 → 双实例竞态。
- **修复**：全局 dsh 回退 **0.1.1-rc.2**（无死循环、ready 行无 token、loopback 免认证）；[launcher.ts](src/launcher.ts) 修复 a) waitForHttpOk 对无 token ready URL 接受任何 <500 响应 b) detectExistingServer marker 扫全量 body；重打 AppImage（21:04）安装并验证——新壳启动即「检测到已运行的 dsh web 实例 http://127.0.0.1:3080/，直接复用」，系统内单实例。
- 验证：tsc 0 错误；79/79 测试通过；备份 `/projects/Backup/Deepseek-Harness/20260906/21_05_01/`（含复用证明日志）。
- **遗留**：`dsh-update.timer` 未禁用，下次自动更新会再引入 rc.1+ 行为变化（浏览器需按新 token 重新认证；shell 自起路径已可靠）；八点/九点性能优化与六点二十修复均未 commit。
- **教训**：① 对常驻服务用 pkill 宽模式（'dsh web'）会误杀用户进程，必须精确匹配 PID/完整 cmdline；② 排查「突然坏掉」先查依赖是否自动更新（版本 mtime + npm 日志），再查自己的改动。
## 六点二十一、2026-09-06 rc.1 升级：死循环证伪 + 壳适配 token 栅栏（代码已被 23 点 40 revert，见六点二十二）

用户要求升回 0.1.2-rc.1（npm 最新即 rc.1，无修复版；22:23 用户已自行重装 rc.1 到全局）。
- **死循环证伪**：`--cpu-prof` 冷启动单实例（6014 采样 5873 idle、ready 行正常）与并发双启动（3978/3979 同时冷启动均正常）都无法复现当晚 100% CPU 循环。六点二十的「rc.1 死循环」应修正为：**事故现场特定状态（pkill 半崩现场+双实例竞态）触发的一次性循环，非 rc.1 稳态 bug**。
- **rc.1 认证模型**（与 rc.2 的差异，壳必须适配）：
  - 根路径无 token → 401 纯文本 `dsh web authentication required; reopen the URL printed by dsh web.`（稳定签名）；带 `?token=` → 303 + `Set-Cookie: dsh-auth-<hash>`（30 天）→ 后续 200+marker。token 每次启动随机、无固定 token 参数、不落盘，只打在 ready 行。
  - 壳自起路径本来就从 ready 行解析 token 并播种，无需改。
- **壳适配**（[launcher.ts](src/launcher.ts) + [main.ts](src/main.ts)）：
  1. `detectExistingServer` 新增 401 分支：body 含 dsh 栅栏签名即判定实例存在（否则 marker-only 探测在 rc.1 下永远判空 → 双实例）。
  2. 新增 `resolveExternalLaunchToken`：`DSH_WEB_TOKEN` 显式指定 → 否则 `journalctl --user -u ${DSH_WEB_UNIT:-dsh-web} --since -48h -o cat` 取最后一个 `token=`；全失败返回 undefined，复用仍进行（窗口落 401 提示页，绝不起第二实例）。
  3. 复用分支导航 `/?token=<t>` 认证播种。
- **验证**：tsc 0 错误；84/84 测试（新增 5 个：栅栏 401 复用×2、token 解析×3）；journal 实测解析出 23:01 的 token；重打 AppImage（23:10）部署至 `~/Applications/DSH Desktop-0.1.0.AppImage` 并 E2E：系统内 `dsh web` 进程=1、复用日志含「已取得 launch token 用于认证播种」、窗口渲染出历史会话页（标题含上次对话）。3080 systemd 服务 23:01 起跑 rc.1，CPU 平稳。
- 遗留：本节改动未 commit；E2E 测试壳带 `--remote-debugging-port=9333` 运行中。

## 六点二十二、2026-09-06 revert rc.1 壳适配（用户报告「能打开但不能用 dsh」）

- 现象：23:20 正常模式重启壳后，窗口能打开但对话不可用。快速取证：dsh-web journal 23:05 后无 error、journal token 仍有效（303）、实例数=1 —— 服务端健康，问题定位不足。
- 处置：用户要求撤回，`git revert 6ada084`（保留历史），壳回到 marker-only 复用 + 自起 token 播种。
- **注意**：revert 后 `detectExistingServer` 对 rc.1 栅栏（401）判空 → 壳会自起实例。**必须停掉 systemd dsh-web 服务**（3080），否则双实例并发写会话存储有 seq 损坏风险（六点二十根因链）。壳自起实例将成为系统唯一实例。
- 复盘线索（下次排查从这里开始）：若壳自起实例后对话仍不可用 → 与壳无关，查模型 API 链路；若可用 → 问题在复用+token 播种路径。
- **真根因（23:50 用户截图定位）**：resume 报 `agent-presets: preset "code" not found`。会话头 `"agentPreset":"code"`（5/13 个会话），rc.1 移除了 "code" 预设（available: standard/ptc/minimal/cordis/code-save/liangshen）。`~/.dsh/.agent-presets/code-save` 存档头自证就是旧 code 预设（8月28日存档）。**与壳的提交完全无关——撤回属误判方向（revert 已做，保持）**。
- **修复（用户级预设目录，一次修全部会话）**：① `cp -a code-save code` 注册 id=code；② 其 `agent.cordis.yml` 的 `tool-presentation` `mode: code` → `mode: ptc`（rc.1 schema 只收 native|ptc|both，旧 code 模式即现在的 ptc）。重启壳后生效。**注意 code-save 存档本身仍是 mode: code，若将来被钉住会同样报错**。
- 环境影响：dsh-web.service(3080) 已停（避免 marker-only 探测下双实例），**socat 8443→3080 的远程浏览器访问随之断开**；恢复方案=壳重做正确的 rc.1 适配（401 分支本身没 bug）后再起 3080。`/projects/dsh-trading` 自带独立 dsh 实例(38537)，与本壳无关勿动。

## 六点二十三、2026-09-07 新建会话界面弹不出：根因=code-save 预设 schema 过期（已修，用户确认恢复）

- 现象：点「新会话」无界面。CDP 排查链：点击生效 → 侧栏出现「新会话」条目 → creator 其实渲染（hero/预设选择器/composer 占位都在 DOM）但 textarea=0、容器宽度塌陷 —— creator 的预设 seat 加载被坏预设卡死。
- **根因**：`~/.dsh/.agent-presets/code-save/agent.cordis.yml` 的 `tool-presentation` `mode: code` 在 rc.1 schema 非法（只收 native|ptc|both）→ roster 挂载失败 → creator 崩。六点二十二里只修了复制出的 `code`，存档 `code-save` 本体漏了。两处均改为 `mode: ptc` 后恢复。
- 排查侧证：rc.1 creator 流程 = `creatorDraft(){ seat.stage("cordis",true); uiWorkspace.startSession() }`（dsh-client-ui-agent-preset），预设表是 creator 的前置依赖。
- 顺带修复：misc-scripts.ts `inputHistoryScript` 的 `findInput()` 用 `.find()` 返回 undefined 而 guard 只查 `!== null` → 每个变更批次抛 `Cannot read properties of undefined (reading 'dataset')`。两处 guard 改 `!= null`。tsc 0 错误、79/79 测试。
- 部署注意：AppImage 已重构建（release/ 00:09），但 `~/Software/DeepSeek.AppImage` 正被运行中壳占用（ETXTBSY）未替换；下次壳重启前手动 cp 或先关壳。运行中的旧壳无碍 —— 预设修复在 `~/.dsh`（服务侧），已即时生效。
- 观察项（未处理）：点击新会话后 2s 内 app 连发 ~877 个 `POST /api/commands/list`（均 200，瞬态，idle 为 0）。若将来出现输入卡顿再查。

## 六点二十四、2026-09-07「查看已归档」按钮图标消失：rc.1 侧栏重构所致（已修）

- 现象：用户反馈归档管理入口没图标。**真根因（00:30 CDP 截图定案）**：插件 `plugins/ui-fixes.css` 的 `.bhn1Oq_sectionHeader > button:first-child{display:none!important}` 本意隐藏 dsh 自带的「查看已归档」native 按钮（rc.2 时代它占 first-child、壳注入按钮排第二，相安无事）；**rc.1 删掉了 native 按钮 → 壳注入的 #dsh-sm-archive-btn 顶成 first-child → 被这条陈年规则误杀**。修正判断：rc.1 的 sectionHeader 仍存在（bhn1Oq_ 前缀，属会话列表模块而非 dsh-client-ui-sidebar，先前仅 grep 单一 bundle 得出「类名已删」结论是错的）。
- 修复：① ui-fixes.css 规则加 `:not(#dsh-sm-archive-btn)` 精确排除壳注入按钮；② 兜底强化（footerActions 图标版锚点 + footArea 图标+文本）保留，作为 rc.x 未来再变结构的保险。
- 排查方法备注：壳被用户重启无 CDP，改用**静态验证**——rc.1 前端 bundle（dsh-client-ui-sidebar/lib/client.js）grep 类名 map（hHd-Xa_*：footArea/footerActions/iconButton/settingsArea/regionArea 等）确认锚点存在性。同类 rc.1 适配可复用此法。
- 部署：tsc 0 错误、79/79 测试、AppImage(00:19) 已更新 ~/Software 与 ~/Applications 两份；用户重启壳后生效。
- 待办移交：① 本节与六点二十三、misc-scripts inputHistory 修复均未 commit；② rc.1 壳适配 revert（22f7b0d）是误判方向，是否重做待用户拍板（重做才能恢复 systemd 3080 + socat 8443 远程访问）；③ dsh-update.timer 仍禁用。

## 六点二十五、2026-09-07 归档入口系列修复（图标尺寸/折叠放置/面板标题与复选框/注入脚本引号事故）

- **图标尺寸**：归档按钮 24→28px、SVG 14→16px（CDP 实测 rc.1 原生 iconButton/headerActions 均为 28x28+16px svg）。
- **折叠态放置（确定性）**：旧逻辑「按钮已存在即跳过」导致首次折叠时按钮随会话列表一起隐藏（rc.1 无原生按钮接管）。新 ensurePanelButton 每 pass 校验位置：展开→工作区 sectionHeader 标签前；折叠→footArea（rc.1 collapsed CSS 明确保留可见：.collapsed .footArea{align-items:center}，仅图标居中样式）；不符即搬。footerActions 中间锚点移除（其父 footArea 更稳定）。
- **面板 UI**：标题「已归档会话」绝对定位水平居中（header justify-content:flex-end 保持右侧按钮）；复选框 appearance:none 自绘毛玻璃（磨砂底+亮边+blur，checked 蓝 #4c9fff+白勾）。
- **引号事故（重要教训）**：session-manage-client.ts 是多层字符串嵌套注入页面的，checkbox 对勾 SVG data-URI 里的 \' 在错误层被吃 → 页面 SyntaxError: Unexpected identifier 'http' → **整个注入脚本全灭**（按钮/面板/菜单注入全挂，用户报「其他界面都有问题」「图标都没了」）。修复：data-URI 内引号一律 %27 编码，零引号字符。**规则：注入脚本的字符串里禁止任何引号字面量穿越嵌套层。**
- **运维教训**：AppImage 运行后 /proc cmdline 变为 /tmp/.mount_*/dsh-desktop-electron，`pgrep -f "DeepSeek.AppImage"` 永远匹配不上真实进程（只会匹配 bash 包装）——找壳进程用 `ps -eo args | grep /tmp/.mount_`；先杀真 pid 再启动，否则新实例撞单实例锁静默退出。
- 验证：79/79 测试；CDP 实测面板 26 行 26 复选框 appearance:none 毛玻璃生效、标题居中、页面零语法错误、截图人工复核通过。AppImage(00:50) 两路径已部署。

## 六点二十六、2026-09-10 删除会话行不消失：rc.1 网关 RPC wire 格式变更（已修，用户确认）

- **现象**：删除会话确认后行不消失，需重启才消失（官方网关归档步骤静默失败）。新建会话不生效同日反馈，实为 0.1.5-rc.1 预设 schema（`text`→`prefix`）问题，修 `~/.dsh/.agent-presets/*/agent.cordis.yml` 并重启壳后 CDP 实测恢复（creator hero/居中 seat 正常渲染）。
- **根因（CDP 网络取证）**：`/api/workspace.archiveSession`（点号路径）在 rc.1 返回 404。rc.1 通用 RPC（dsh-client-connection `createWebConnectionRpc`）wire 变为：`POST /api/<channel>/<endpoint>` 斜杠路径 + envelope `method` 字段同为斜杠 + payload 必须 `{ args: { <descriptorArgName>: {...} } }`（本例 descriptor 参数名 `request`，且「exactly one plain-object args field」校验）。官方命令 id 形如 `@deepseek-ai/dsh-api-workspace-controller#workspace/archiveSession`。
- **修复**（[session-manage-client.ts](src/session-manage-client.ts) `rpcArchive`，仅 1 处）：URL `/api/workspace.archiveSession`→`/api/workspace/archiveSession`；`method` 同改斜杠；payload `{ sessionId }`→`{ args: { request: { sessionId } } }`。响应 envelope `result.ok===true` 判定不变、兼容。src 全库仅此一处点号 RPC 调用。
- **验证方法（可复用）**：页面内 fetch 探测路由（非法 sessionId 得 `session/not-found`=路由全通，404=路由死，`gateway/arguments-invalid`=参数形状错）；热注入编译产物 `Runtime.evaluate(lib 产物字符串)`（脚本自带 dispose 换实例，免重启壳）；真实鼠标序列点击（mouseMoved 预热 120-150ms → pressed → 80ms → released）才能触发官方菜单 + pointerdown 记录 `lastEllipsisRow`（合成 `el.click()` 不产生 pointerdown → 注入项不出现，测试假阴性）；测试前先 Escape 关残留菜单（防切换关闭）、关掉遮罩面板（挡点击致 rect(0,0)）。
- 测试：tsc 0 错误；79/79 通过；用户实测删除即时生效「删除可以了」。备份 `.backup/20260910_delete-session-api-rc1/`。未 commit（待用户指示）。
- 备注：dsh 全局版本 0.1.5-rc.1（systemd dsh-web 3080 与壳自起实例并存，双实例写风险见六点二十二，未处理）。

## 六点二十七、2026-09-10 composer 统计弹窗毛玻璃化（bRhRbq_panel，未 commit）

- **需求**：输入框下方统计行的两个弹窗（「1 轮 1 步」「缓存命中」，点击弹出详情面板）也要毛玻璃，归 设置→主题设置→界面毛玻璃→弹出层 滑块控制。
- **定位（CDP）**：两弹窗同类 `bRhRbq_panel`（300 宽，composer 上方；模块其余类 title/titleLabel/titleValue/titleRule/details 不含 panel 子串，子串选择器无误伤）。DSH 原生平涂 rgba(39,46,62,0.14) 无 blur。
- **修复**（[glass.ts](src/glass.ts) ambientStyleScript 弹出层家族规则，约 L763）：选择器列表追加 `[class*="bRhRbq_panel"]`，获得 `background-color: var(--dsh-glass-popup-bg)` + `backdrop-filter: var(--dsh-glass-popup-filter)`，与其他弹出窗同配方。
- **验证**：tsc 0 错误；79/79 测试；页面 `dsh-dt-style` 节点热补丁（选择器列表前插同名选择器）后 computed bg/blur 即变为滑块值（用户滑块当时 14%→bg 0.14+blur(20px) saturate(1.4)）；变量联动实测——临时改 `--dsh-glass-popup-bg`=0.5/`--dsh-glass-popup-filter`=blur(40px) 后面板立即跟随，移除内联变量后回滑块值。热补丁即当前窗口即时生效；lib 已构建，重启后从源码注入同款规则。
- 注意：`#dsh-glass-custom` 节点只存 5 个 CSS 变量声明；ambientStyleScript 的规则全集在 `#dsh-dt-style` 节点（热改规则要改对节点）。备份 `glass.ts.stat-popup`（同目录 20260910_delete-session-api-rc1/）。

## 六点二十八、2026-09-10 「弹出层」滑块更名「弹窗」（未 commit）

- [misc-scripts.ts:805](src/misc-scripts.ts#L805) glassControlsScript labels：`popup: '弹出层'`→`'弹窗'`（英文 'Popup menus' 与其余滑块标签不动；「弹窗模糊」本就叫弹窗模糊，改名后两者前缀一致）。改注入脚本源头，非 DOM 替换（React 恢复教训）。
- 验证：tsc 0 错误；79/79 测试；编译产物热注入（glassControlsScript 自带 `__dshGlassControlOff` dispose 可安全重注入）后打开主题设置面板实测标签=「弹窗」。备份 `misc-scripts.ts.popup-label`。

## 六点二十九、2026-09-10 打开设置界面「先暗一下」（未 commit）

- **根因（CDP 帧采样）**：设置面板毛玻璃规则挂在 `[data-dsh-settings-panel]` 上，而打标由 themeSettingsScript 的 rAF 合批 healTick 执行——面板打开后前 2-3 帧（实测 t=0~6ms）以 DSH 原生不透明 `rgb(32,38,52)` 渲染，t≈57ms 打标后才变 `rgba(15,17,23,0.05)`+blur → 视觉上先暗一下再变亮。mut-bus 合批让 rAF 打标必晚于首帧。
- **修复**（[glass.ts:975](src/glass.ts#L975)）：选择器改 `[class*="VOzbGW_panel"]`（类直击，首帧命中；CDP 确认 VOzbGW 模块含 panel 的类仅此一个，无误伤）。themeSettingsScript 的打标保留但不再 load-bearing。
- **验证**：热补丁 `dsh-dt-style` 后重采样：首帧 t=5 `tagged=null` 时 bg 已是 0.05 半透明。tsc 0 错误；79/79 测试。备份 `glass.ts.settings-flicker`。
- 测试技巧沉淀：设置 trigger 的 `el.click()` 打不开面板（React 需真实指针事件）；帧采样要在页面内 rAF 循环做（CDP 往返延迟 >16ms 会漏帧）。

## 六点三十、2026-09-10 DSH 官方右侧边栏无光标粒子（未 commit）

- **现象**：点击右上角「打开右侧边栏」后，弹出的侧边栏上光标无粒子效果（其他区域正常）。
- **根因**：dsh 0.1.5-rc.1 新增官方右侧边栏（`data-sidebar-right-expand`/`data-sidebar-right-toggle` 按钮，容器 `section._pane_17p4l_133`），它浮动在 `_sidebarCol`/`_centerCol` 布局之外——whaleSprayScript 的 `triggerZone` 只认这两个布局锚 + 壳的 dsh-files-panel，光标落在官方 pane 上时 zone=null → 无粒子。注意壳的 files-panel 按钮与官方 expand 按钮都在右上角区域，别混淆（官方按钮 aria-label 也叫「打开右侧边栏」）。
- **修复**（[misc-scripts.ts:104](src/misc-scripts.ts#L104) triggerZone）：filesPanel 分支后新增 `section[class*="_pane_"]` 检测，命中归 'sidebar' 模式（星星）。CDP 确认 `_pane_` 子串全页面唯一（仅右侧 pane），无误伤。
- **验证**：tsc 0 错误；79/79 测试；热注入新 whaleSprayScript（自带 dispose 可重注入）后 CDP 真实鼠标在 pane 内移动 canvas 粒子像素 340→306 动态变化（修复前恒 61 残留）。备份 `misc-scripts.ts.right-pane-fx`。
- 取证技巧：粒子存活检测用页面 canvas `getImageData` 非透明像素计数；粒子 ttl <1s，移动后立即读，隔 150ms 再读对比确认动画在跑。

## 零点五十、2026-09-11 官方右侧边栏：置顶让位/毛玻璃圆角/竖线（未 commit）

- 用户四连需求：①设置面板要盖过右侧栏 ②右栏透明度跟随背景滑块 ③右栏四周圆角 ④右栏左侧竖线去掉；随后纠正①：设置打开时右栏应**整体让位**（毛玻璃面板会透出右栏，盖在上面不行）。
- **实现**（[glass.ts:930](src/glass.ts#L930)、[misc-scripts.ts:1199](src/misc-scripts.ts#L1199)）：
  - `section[class*="_pane_"]` 纳入主玻璃家族（bg=主滑块 + ::before 列模糊）+ 16px 圆角/8px margin/overflow hidden 浮动卡片。
  - 竖线=右栏列分隔拖拽条。**坑**：`pI_x6G_handle` 是通用类（左右两条），全局 display:none 破坏布局（当时误判为 composer 塌，AB 测试证明 composer 全宽实为官方窄窗口响应式，与 handle 无关）。handle 是 frame 的**最后**子元素（DOM 顺序≠视觉顺序，`:has(+col)` 不匹配），最终规则：frame 含 rightbarCol 时隐藏 `:last-child` handle；另去 `P3OORG_panel` 1px 左边框。
  - 设置面板打开时右栏让位：healTick 检测 VOzbGW_panel 出现/消失，在 `<html>` 上切 `data-dsh-hide-pane` 标记，CSS `display:none` 联动（不模拟点击官方按钮——el.click() 对它们无效，需真实指针事件）。SPA 的「pane 已开」状态不受影响，关设置即回来。
- **验证**：flag 切换 display none↔flex ✓；竖线消失、圆角毛玻璃生效（截图）✓；tsc 0 错误 79/79。
- **事故与恢复**：Emulation.setDeviceMetricsOverride 模拟宽视口验证后界面渲染错乱（用户：「界面全乱了」）——重启壳恢复（lib 已含全部修复，重启即最终形态）。**教训：真实窗口验证不要用 Emulation 视口，它会搞乱 niri 平铺下的真实窗口渲染**。
- 壳重启方法（无托盘场景）：kill electron 主进程后须带完整图形 env（XDG_RUNTIME_DIR/WAYLAND_DISPLAY=wayland-1/DISPLAY=:0/HOME/PATH 含 ~/.local/bin 供 dsh 命令）+ --remote-debugging-port=9333 启动；env 缺失会 Wayland 连接失败或 spawn dsh ENOENT 退出。niri 平铺下窗口宽度由布局决定（当前 1714），composer 贴底全宽为官方窄窗口响应式。

## 一点十分、2026-09-11 pane 被永久隐藏修复（未 commit）

- **现象**：用户报「右边侧边栏里面是空的/弄坏了」——实为 `data-dsh-hide-pane` flag 残留 "1"，pane 整体 display:none（内容 245 元素一直都在）。
- **根因**：flag 判定源用了 healTick 的 `settingsPanel = list.closest('div[class*="_panel"]')`——该子串 closest 也命中**常驻**面板表面 → settingsPanel 恒非 null → healTick 每次都重设 flag，清不掉。另一坑：healTick 由 mut-bus(rAF) 驱动，**窗口后台时 rAF 暂停**，热注入后手动 poke 突变也不跑；真实场景安全（关设置必在前台点击）。
- **修复**（[misc-scripts.ts:1208](src/misc-scripts.ts#L1208)）：flag 判定源改为 `VOzbGW_overlay`（设置面板挂载容器）的存在性——面板关 = unmount = null = 清 flag，无误报源。
- **教训**：注入脚本模板字符串注释里**严禁反引号**（截断模板串 → TS1005，本 round 踩过又忘）；宽子串 closest（`_panel`）作状态判定源必然误报，要用唯一挂载标志。
- **验证**：tsc 0 错误、79/79；热注入后手动清 flag，pane flex/755w 恢复、内容完整；新判定链路（overlay null → 清 flag）逻辑闭合。备份 `misc-scripts.ts.flag-fix`。

## 一点二十五、2026-09-11 pane 隐藏改纯 CSS :has（flag 方案废弃，未 commit）

- VOzbGW_overlay 判定版 flag 仍残留了一次（面板关后窗口切后台 → rAF 暂停 → 清 flag 的 healTick 不跑）→ 用户再次报「侧边栏空的」。
- **终案**：删掉 JS flag 全套（[misc-scripts.ts:1199](src/misc-scripts.ts#L1199) 仅留注释），glass.ts 改纯 CSS：`[class*=pI_x6G_frame]:has([class*=VOzbGW_overlay]) [class*=_pane_] { display:none !important }`——overlay 挂在 frame 内（sidebarCol→footArea→settingsArea），:has 后代可见；面板卸载即恢复。声明式、零时机依赖、后台窗口也可靠。
- **验证**：重启壳后页面注入新规则、无旧 flag 规则/属性；页面内模拟 overlay 挂载 → pane display:none，移除 → 恢复 ✓。tsc 0 错误、79/79。备份 `glass.ts.pane-css-has`、`misc-scripts.ts.flag-removed`。
- **教训**：UI 联动优先声明式 CSS（:has 已成熟）；JS 状态 flag + rAF 驱动的清理在窗口后台场景必然脆弱。

## 一点四十五、2026-09-11 设置面板右侧可点 + 拖拽调宽恢复（未 commit）

- **点击拦截根因**：右栏外壳 `P3OORG_panel`（position:absolute）带官方 `z-index:10`，而左栏是 `z-index:1`——设置面板 fixed overlay 的 2147482000 只在左栏 z:1 stacking context 内有效，出不来；右栏透明 surface（z10>z1）盖在面板右侧吃掉所有点击。修复：`[class*="P3OORG_panel"] { z-index: 0 !important }`（与 border-left:none 同规则）。
- **拖拽调宽恢复**：用户反馈右栏不能调宽——竖线真源其实是 P3OORG_panel 的 1px border-left（已除），`pI_x6G_handle` 本身零宽无视觉、纯拖拽热区，**不必隐藏**；撤销上一轮的 handle display:none，两条 handle 全部保留。
- **注入事故纠正**：`ambientStyleScript()` 返回的是自管理 IIFE 脚本（自移除/自建 #dsh-dt-style），**必须 eval 执行**；曾错把它当 CSS 塞 textContent——CSS parser 把数组尾逗号后的内容当未终止字符串吞掉，页面半生效（部分规则丢失但难察觉）。正确姿势：`(new Function(script))()`，注入后 styleLen 应 ~14k 纯 CSS（非 74k JS 文本）。
- **验证**：shell z=0/border 0、双 handle 在、设置面板右侧 85% 处 elementFromPoint 命中面板内部主题选择器；截图确认设置居中完整+右栏让位+遮罩正常。tsc 0 错误、79/79。备份 `glass.ts.final`。

## 两点整、2026-09-11 右栏顶部标签改浅色毛玻璃（未 commit）

- pane 顶部 tab（`_tab_17p4l_156`）官方背景 rgb(44,44,46) 暗黑色 → 加入弹出层家族：`--dsh-glass-popup-bg` + `--dsh-glass-popup-filter`，跟随主题设置「弹窗」滑块。`[class*="_tab_"]` 子串全页唯一（`_tabActive_` 不含 `_tab_` 子串），无误伤。
- 验证：computed bg rgba(39,46,62,0.07) + blur(20px)（=滑块当前值）。tsc 0 错误、79/79。备份 `glass.ts.tab-glass`。

## 二十二点十、2026-09-11 确认/选择弹窗与问题卡片玻璃化（未 commit）

- **根因**：对话框背景全部走 `--dsw-alias-bg-layer-2`，而 SURFACE_DARK 把它钉死为不透明 rgb(32,38,52)（L69 注释 dialog panels, pills）；且官方 `_dialog_w1urq_22` 规则只有 background 无 backdrop-filter。确认卡片 `_confirmation_1nu42_1`（1nu42 模块）自身无 background 规则、经由同一 alias。
- **修复**：SURFACE_DARK/LIGHT 的 bg-layer-2 → `var(--dsh-glass-popup-bg, fallback)`（popup 玻璃变量主题无关、两分支共用）；补 `[class*="_dialog_"], [class*="_confirmation_"]` 的 popup-filter（子串在 CSS bundle 中唯一，无 _xxx_dialog_ 变体）。
- **取证路径**：GUI bundle 在 `@deepseek-ai/dsh-web-frontend/dist/assets/index-*.css`，类名哈希只能从 CSS 挖（client-ui-* 包的 client.js 是 CSS modules 引用，无字面类名）。
- **验证**：新 AppImage（重 dist → cp 到 ~/Software/ → 带 --remote-debugging-port=9333 启动）；styleReady+dialogRule+layer2Var=rgba(39,46,62,0.070)；测试节点 computed backdropFilter=blur(20px) saturate(1.4)（=滑块值）。tsc 0 错误、79/79。备份 `glass.ts.dialog-glass`。
- **AppImage 要点**：壳进程 cmdline 是 /tmp/.mount_*/dsh-desktop-electron；用户从 ~/Software/DeepSeek.AppImage 启动，更新产物后要 cp 过去；CDP 端口需启动参数显式传。

## 二十二点半、2026-09-11 全量浮层毛玻璃统一（未 commit）

- **排查方法**：CSS bundle（dsh-web-frontend/dist/assets/index-*.css）穷举 `position:fixed` + 浮层语义类名（dialog/modal/overlay/popover/menu/toast/confirm/float…），逐类查背景定义，对照 glass.ts 已覆盖面找缺口。
- **六类弹窗全部归入「弹窗」滑块**（popup-bg + popup-filter，CDP 测试节点实测 blur(20px) saturate(1.4) 全命中）：
  1. `_dialog_w1urq_22` 通用对话框（上轮已修）
  2. `_confirmation_1nu42_1` 确认/问题卡片（上轮已修）
  3. `_float_17p4l_306` pane 浮动面板——bg 已随 layer-2 alias 玻璃化，本轮补 blur
  4. `_onboardingStage_1cfrq_21` 引导浮层——bg 走 layer-1 动态玻璃，本轮补 blur
  5. `_toast_e5v0f_6` toast 通知——原不透明 contrast-fill，本轮改 popup-bg；inverted 白字保留（7% 深蓝玻璃叠深色页面背景，白字对比不受影响）
  6. `_bubble_1nw3t_1` tooltip 气泡——官方半透明深色 0.62 底，本轮补 blur（深色毛玻璃，白字可读）
- **不动项**：`_portal_`/`_copyAnchor_`（无背景）、`_onboardingMask_`（官方 blur(2px) 全屏遮罩，太重不宜 20px）、`_mask_w1urq_`（官方 --dsw-mask-blur）、代码块 `_banner_`（内容元素非弹窗）。
- **子串安全**：`_float_` 不匹配 `_floatResize/Title/Body/Header`（下划线位置）；`_toast_`/`_bubble_1nw3t`/`_onboardingStage_` 唯一。
- **验证**：tsc 0 错误、79/79；AppImage 重打包完成；~/Software/ 副本因运行中文本忙，后台 watcher（/tmp/update-appimage.sh）待壳退出后自动覆盖，完成写入 /tmp/appimage-update.log。备份 `glass.ts.all-floats`。

## 二十三点整、2026-09-11 右侧栏默认宽度减半（未 commit）

- **现状**：pane 外壳 `P3OORG_panel`（absolute）inline width 771px（≈窗口 45%），React 写死，无 settings/localStorage 配置项；CSS !important 会锁死宽度废掉拖拽。
- **方案**（[misc-scripts.ts:39](src/misc-scripts.ts#L39) paneWidthScript，main.ts injectBatch 注册）：mut-bus 驱动 tick 对 **inline width** 操作——基线模型：首次观察值=baseW（官方默认）；width 在 baseW±8px 内 → 砍成 baseW/2；handle 上 pointerdown 标记 dragSeen，拖拽结束后新宽度成为新 baseW（用户拖拽不被再砍），React 回弹默认值则再砍。
- **验证**：771→386px（砍半生效，React 未回弹）；tsc 0 错误、79/79。AppImage 重打包；后台 watcher v2（/tmp/update-appimage2.sh，ps 锚定 ^/tmp/.mount_ 精确匹配避免 pgrep 自匹配陷阱）等壳退出后覆盖 ~/Software 副本。
- **教训**：热注入后 0.9s 断言太早（mut-bus rAF 链路有延迟）——验证窗口后台/回调链路要给足时间或两步探测。

## 二十三点四十、2026-09-11 右侧栏 push 空白消除（未 commit）

- **根因**：官方 centerCol 让位量 771px 烘焙在 CSS-in-JS 运行时规则里（布局类 pI_x6G_* 不在 dist CSS 文件，grep 不到是正常的——在页面 100+ inline stylesheets），不跟随面板实际宽度；面板砍半到 386 后 centerCol 与面板间死出 393px 空白条。
- **修复**（[glass.ts:1020](src/glass.ts#L1020) 纯 CSS）：`:has()` 检测面板可见（:not aria-hidden）→ centerCol `margin-right: -385px`（779 总让位 - 386 面板 - 8 自身 margin），右缘精确贴面板左缘。官方无过渡（transition 0s），margin 跳变一致。
- **验证**：gap=0（center 280..1328 = pane 左缘 1328）；tsc 0 错误、79/79。
- **局限**：面板被拖离 386 后空白条等量重现（官方让位 state 固定）；砍半基线稳定场景下可接受。
- **坑**：右上角有两个右栏按钮——`data-sidebar-right-expand`（1684,14 关闭态）与 `data-sidebar-right-toggle`（面板内，跟随面板 transform 可滑出窗口外 2058,18）；自动化点击按 data 属性定位，别取「最后一个」。

## 二十三点五十、2026-09-11 侧栏拖拽手柄错位修复（未 commit）

- **用户报告**：右上角侧边栏不能调整宽度了；拉的话会导致左侧侧边栏图标移动。
- **根因**（CDP 实验证实）：frame 渲染**两个** `pI_x6G_handle` col-resize 条——左栏自己的在 DOM **前**、右面板的在**后**（首个拖之→sidebarCol 变宽，末个拖之→pane 变宽）。paneWidthScript 的 sync/pointerdown 全用 `querySelector`（取首个）→ 把**左栏手柄**搬到了面板左缘 1320：用户在面板边缘抓到的是左栏手柄，一拖左栏图标全动、面板本身永远调不了宽度；React 真正的面板手柄滞留在 943（winW−771 未砍半 state）浮在会话区中央。
- **修复**（[misc-scripts.ts:60](src/misc-scripts.ts#L60)）：
  1. `paneHandle()` = handle 列表**最后一个**且仅当 ≥2 个时（收起态唯一手柄是左栏的，绝不能碰——旧代码收起时把它推到 winW 导致左栏在面板关闭时无法拖宽）；
  2. pointerdown dragSeen 仅当目标是 paneHandle；
  3. 手柄定位公式从 winW−w−8 改为 winW−w 与 React 完全一致（React state=真实宽度后两写方同值，消除抖动）；
  4. 新增 attribute-scoped MutationObserver（panel+frame 的 style，rAF 去抖）：React 报告拖拽只改 inline style（纯 attribute churn），mut-bus 不监听 attributes → 旧代码 paneGive 滞后一拍，拖完 grid 第三列停在旧值，面板旁重现空白条；
  5. applyW 同值不写（防 attrObs 自触发死循环）。
- **验证**（CDP 全场景）：打开→双 handle 各就各位（rail 276 / pane 1328）grid 280/1040/394 无空白；拖 pane 边 1328→1100→pane 386→614、rail 268 纹丝不动、paneGive=622 实时跟随；干净态拖 rail→rail 348、pane 386 不变、center 守恒；收起→center 填满、唯一手柄在 rail 缘 360、右上角图标(1684,14)可见；重开→宽度记忆 614 保持。tsc 0 错误、79/79。
- **坑**：drag-test 合成拖拽把 release 放中心区会触发官方 click-outside 关面板（测试 artifact，非 bug）；拖 pane 后 React state=真实值，此时手柄/内联 grid 全部自洽，glass 覆盖仅在砍半默认场景起作用。

## 二十点三十、2026-09-12 双 dsh web 实例撞 session.lock 致「不能选择模型」（未 commit）

- **用户报告**：本项目不能选择模型了。CDP 复现：模型菜单能弹（0.1.5-rc.1 二级结构 `_7KE1Ra_menu` → 模型/推理等级 cell → `_7KE1Ra_option` 列表），onClick 已挂且被触发（spy 实证），调 `choose()` 后 POST `/api/session/selectModel` 返回 200，但 body `result.ok=false: SessionAlreadyOwnedError: session ... is already owned by an active write handle` → UI 静默不改选中、菜单不关。
- **根因**：双 dsh web 实例共享 `~/.dsh/sessions`。上游 dsh 9/10 自动升级 **0.1.5-rc.1**（token 栅栏：tokenless GET / → 401+固定签名文本），`detectExistingServer` 只认 200+`__DSH_BOOT__` → 判空 → 壳自启第二实例（`--port 0`）；恰逢今早开机 systemd `dsh-web.service`（3080）自启并持有活跃会话的 `session.lock` 写句柄，壳实例（35069）的 selectModel resume 撞锁全灭。等价于 revert 22f7b0d 曾修过的「当晚事故机制之一」。
- **诊断路径**（复用价值高）：`el.click()` 无效→排除事件拦截；原生 3080 页同操作成功→锁定壳注入/环境；网络抓包见 RPC 200 但 ok=false→服务端拒绝；`ls -l /proc/<pid>/fd | grep session.lock` 实锤 3080 实例持锁；`curl` 401 体含 `dsh web authentication required` 签名。
- **修复**（[launcher.ts](src/launcher.ts) + [main.ts](src/main.ts) boot 复用分支）：
  1. `detectExistingServer` 新增 401 分支：候选回 401 且 body 含 `DSH_AUTH_SIGNATURE`（'dsh web authentication required'）→ 判活实例 `{url, authRequired:true}`（签名而非裸状态码，防误判其他 401 服务）；返回类型 `URL|undefined` → `ExistingServer|undefined`。
  2. 新增 `resolveExternalLaunchToken`：`DSH_WEB_TOKEN` 显式指定 → journalctl `--user -u ${DSH_WEB_UNIT??'dsh-web'} --since -48h -o cat` 扫 ready 行取最后一个 `?token=`（重启留陈旧 token，最新所属当前进程）。
  3. 新增 `validateInstanceToken`：GET `/?token=<t>` redirect:manual，303（种 cookie）/200 为有效。
  4. boot 复用分支：authRequired 时解析+校验 token → 以 `/?token=<t>` 导航播种持久 cookie；token 失败仍复用（窗口落 401 提示页，**绝不起第二实例**）。
- **验证**：tsc 0 错误、88/88（新增 9）；重启壳日志「token 栅栏实例，经 http://127.0.0.1:3080/?token=… 播种认证 cookie」「检测到已运行实例…直接复用」；仅 3080 一个 dsh web 监听；CDP E2E 真鼠标点 DeepSeek-V4-Pro → aria-label 变更、菜单关闭，随后还原 V41-Flash。AppImage 尚未重打包。
